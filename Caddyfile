{
    email chapmlinks@gmail.com
    auto_https off
}

# Synapse Admin UI
admin.sky0cloud.dpdns.org:80 {
    reverse_proxy sky0cloud-synapse-admin:80
}

# Main domain (Element + Matrix API)
sky0cloud.dpdns.org:80 {

    # Global CORS
    header {
        Access-Control-Allow-Origin *
        Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS"
        Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept, Authorization, X-Matrix-User-ID"
        Access-Control-Expose-Headers "Content-Length, X-Matrix-User-ID"
    }

    # Matrix discovery
    handle /.well-known/matrix/client {
        respond `{"m.homeserver":{"base_url":"https://sky0cloud.dpdns.org"}}` 200 {
            header Content-Type application/json
        }
    }

    handle /.well-known/matrix/server {
        respond `{"m.server":"sky0cloud.dpdns.org:443"}` 200 {
            header Content-Type application/json
        }
    }

    # Matrix API paths
    @matrix path /_matrix/* /_synapse/* /_admin/*

    reverse_proxy @matrix {
        to sky0cloud-synapse:8008 synapse-client-worker:8008 synapse-federation-worker:8008
        header_up Host {host}
        header_up X-Real-IP {remote_host}
        header_up X-Forwarded-For {remote_host}

        # Remove CORS headers from upstream to avoid conflicts
        header_down -Access-Control-Allow-Origin
        header_down -Access-Control-Allow-Methods
        header_down -Access-Control-Allow-Headers
    }

    # Element web client (fallback)
    reverse_proxy / {
        to sky0cloud-element-web:80
    }
}
